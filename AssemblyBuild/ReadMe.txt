
If you wish to use Zenject as a DLL rather than including the source with every project, you can build it by compiling the solution provided in this folder.

The files should appear in a Bin directory in this same directory.  Note that you will need to put the Zenject-editor.dll in a folder named Editor/
